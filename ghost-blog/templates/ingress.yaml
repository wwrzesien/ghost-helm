apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  # annotations:
  #   nginx.ingress.kubernetes.io/proxy-body-size: "0"
  name: "{{ .Values.name }}"
spec:
  rules:
    - host: ghost-app.192.168.99.100.nip.io
    - http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: "{{ .Values.name }}"
                port:
                  number: 2368
